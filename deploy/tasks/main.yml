- name: Clone a repo with separate git directory
  ansible.builtin.git:
    repo: https://github.com/nathanforester/DockerComposeTutorial.git

- name: docker compose
  shell: docker compose -f /home/ubuntu/DockerComposeTutorial/APrimeAge/docker-compose.yaml up -d